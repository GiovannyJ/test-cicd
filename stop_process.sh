#!/bin/bash

# Find and kill the process by name or other criteria
# For example, if your Go application is named "app"
# Replace "app" with the actual process name or criteria
pkill -f "app"

# You can add more commands if necessary
# For example, to handle additional cleanup tasks
# ...

echo "Process stopped."